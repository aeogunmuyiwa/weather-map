<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>weather map</title>
    <link rel="stylesheet" type="text/css" href="/css/style.css">
    <link href='https://fonts.googleapis.com/css?family=Open+Sans:300' rel='stylesheet' type='text/css'>
  </head>
  <body>
    <div class="container">
      <fieldset>
        <form action="/weather" method="post">
          <input name="city" type="text" class="ghost-input" placeholder="Enter the latituede" required>
           <input name="country" type="text" class="ghost-input" placeholder="Enter the Longitude" required>

          <input type="submit" class="ghost-button" value="Get Weather">
        </form>
        <% if(weather !== null){ %>
          <p><%= weather %></p>
        <% } %>

        <% if(error !== null){ %>
          <p><%= error %></p>
        <% } %>
      </fieldset>

    </div>
     <div class="container">
       <form action="/logout" method="get">
        <input type="submit" class="ghost-button" value="logout">
         </form>
    </div-
  </body>
</html>

